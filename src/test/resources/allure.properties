# Allure report configuration
# Placed on the test classpath so Allure picks it up automatically.

# Directory where raw JSON results are written during test execution
allure.results.directory=target/allure-results

